import { AppProvider } from "../AppProvider";
import { SignupForm } from "../SignupForm";
export function SignupPage() { return <AppProvider><div className="flex min-h-screen items-center justify-center bg-bg"><SignupForm /></div></AppProvider>; }
